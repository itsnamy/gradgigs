import 'package:flutter/material.dart';

class RecruiterViewBasicInformation extends StatelessWidget {
  const RecruiterViewBasicInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
